# CLAUDE.md

## 项目名称
查课 App

## 项目目标
这是一个面向课堂查课场景的 Flutter App，第一版优先 Android，核心功能包括：

1. 点名
2. 记名
3. 查课记录
4. 本地优先保存与服务器同步
5. 应用异常退出后的任务恢复
6. 总群汇报文本生成
7. 学委文本生成

第一版暂不实现登录，但代码结构必须为后续登录、多用户、权限控制预留扩展位。

---

## 工作方式要求

### 总原则
- 先规划，后实现
- 先做可运行骨架，再逐步补功能
- 先保证架构正确，再优化 UI
- 不要一上来生成巨型 demo 文件
- 不要把复杂业务逻辑堆在页面层
- 每完成一个阶段都必须能运行、能验证

### 当前阶段优先级
当前优先做：
1. 项目规划与数据设计
2. Excel 名单导入数据库脚本
3. MySQL 基础表设计
4. GitHub 仓库结构与规则
5. Flutter 本地数据库与任务系统骨架

在我没有明确要求前，不要直接铺开全部 Flutter 页面实现。

---

## 技术栈约束

### 前端
- Flutter
- Riverpod
- go_router
- Dio
- Drift(SQLite)
- freezed + json_serializable
- feature-first 目录结构
- Android 优先

### 后端
- FastAPI
- MySQL
- SQLAlchemy
- Alembic

### 数据导入
- Python 脚本
- 从多个 Excel 文件导入 MySQL
- 支持字段清洗、去重、拼音生成、归类导入

### 版本控制
- Git
- GitHub
- 使用 monorepo 结构

---

## 仓库结构要求

使用 monorepo：

- app/         Flutter 客户端
- server/      FastAPI 服务端
- scripts/     Excel 导入脚本、数据处理脚本
- docs/        需求、架构、流程、数据设计文档

---

## 核心业务要求

### 首页
首页包含三个入口：
- 点名
- 记名
- 查课记录

### 点名模块
#### 点名选择页
- 年级选择
- 专业选择
- 班级选择
- 返回
- 开始点名

#### 点名执行页
- 当前学生姓名
- 姓名拼音
- 当前学生所属专业/班级
- 下一位
- 返回
- 结束查课
- 若多个班学生混合，班级信息需随当前学生实时变化

### 记名模块
#### 记名选择页
- 年级选择
- 专业选择
- 班级多选
- 开始记名

#### 记名执行页
- 多个班级左右滑动切换
- 当前班级显示
- 当前学生状态显示：
  - 待查
  - 已到
  - 未到
  - 请假
  - 其他
- 状态颜色区分
- 表格显示姓名、学号
- 一排两人
- 表格区域内部可滚动
- 点击学生块可切换当前焦点
- 底部按钮：
  - 缺勤
  - 请假
  - 其他
  - 下一位（表示已到）
- 右上角确认名单
- 页面显示进度：
  - 全部待查人数
  - 已处理人数

#### 记名确认页
- 只展示异常名单：
  - 未到
  - 请假
  - 其他
- 按班级分组
- 字段：
  - 姓名
  - 完整学号
  - 状态
- 重新编辑
- 确认名单

#### 文本生成
- 总群汇报文本生成
- 学委文本生成
- 文本模板必须可配置，不允许写死在代码中
- 若用户未生成文本直接结束，使用弹窗确认，不要用死板锁按钮方案

### 查课记录
- 展示每次查课记录
- 支持查看和编辑
- 编辑后重新保存
- 必须保留修改时间
- 支持文本再次生成
- 支持重新同步服务器

---

## 架构原则

### 1. 统一任务系统
点名和记名必须统一抽象为“查课任务系统”，不能做成两套完全割裂的逻辑。

### 2. 本地优先
所有查课操作都必须先写本地数据库，再异步同步服务器。

### 3. 离线可用
无网状态下必须能正常使用点名、记名、记录查看和任务恢复。

### 4. 中断恢复
应用启动时必须检查是否存在未完成任务，若有则提示继续或放弃。

恢复内容至少包括：
- 当前任务类型
- 当前页面层级
- 当前班级
- 当前学生
- 已处理状态
- 是否已确认名单
- 是否已进入文本生成阶段

### 5. 模板可配置
总群汇报和学委文本模板必须配置化，不允许硬编码在页面中。

---

## Excel 数据导入规则

### 当前已知情况
当前 Excel 数据并不规范，通常只有：
- 学生姓名
- 学号

而以下信息可能出现在：
- 文件名
- sheet 名称
- 表头
- 前几行说明文字
- 其他位置

### 处理原则
必须把“Excel 导入数据库”作为开发第一阶段优先任务。
不要假设 Excel 已标准化，必须设计具备一定容错能力的导入脚本。

### 导入脚本必须满足
1. 扫描指定目录下所有 Excel 文件
2. 支持 xlsx / xls
3. 自动分析每个 Excel 文件和 sheet 的结构
4. 自动尝试从文件名、sheet 名、表头、前几行文本中提取：
   - 年级
   - 专业
   - 班级
5. 自动识别学生数据区域
6. 自动识别“姓名”列和“学号”列
7. 若无拼音字段，自动生成姓名拼音
8. 做字段清洗
9. 做重复校验和学号唯一性检查
10. 支持 dry-run
11. 支持重复执行避免重复插入和脏数据
12. 生成导入日志和异常报告
13. 对无法确定的信息输出“待人工确认”结果，不允许静默跳过

### 导入脚本设计要求
- 先分析 Excel 结构
- 再提出推荐模板规范
- 再设计导入流程
- 优先保证稳健性和可追踪性

---

## 代码规则

### 页面层规则
- 页面层禁止直接操作数据库
- 页面层禁止直接写复杂业务逻辑
- 页面层只负责展示、事件分发、简单交互

### 业务层规则
- 核心逻辑进入 controller / notifier / service / usecase 层
- 点名与记名共用统一任务模型
- 同步逻辑必须集中管理

### 数据层规则
- Repository 统一管理数据访问
- 本地数据库与远程接口访问必须解耦
- 同步队列必须独立建模
- 记录编辑后必须更新修改时间

### 输出要求
- 不要把所有内容塞进单个文件
- 必须按模块拆分
- 每阶段都应可运行、可验证
- 优先保证架构正确，再优化 UI

---

## GitHub 规则

### 分支建议
- main：稳定版本
- dev：开发集成
- feature/*：功能开发
- fix/*：问题修复
- docs/*：文档更新

### 提交规范
采用 Conventional Commits 风格，例如：
- feat: add attendance task domain models
- fix: repair task resume logic
- refactor: split sync service from repository
- docs: add excel import specification
- chore: init flutter and fastapi workspace

### 禁止提交
- 原始学生 Excel 数据
- .env
- 本地数据库文件
- 构建产物
- 日志文件
- 临时测试数据
- IDE 私有配置

---

## 实施顺序要求

优先实施顺序：
1. Excel 导入脚本 + MySQL 初始化
2. 基础数据模型与服务端表设计
3. Flutter 本地数据库与任务系统骨架
4. 首页和路由
5. 点名流程
6. 记名流程
7. 确认页与文本生成
8. 记录系统
9. 同步与恢复
10. UI 与异常处理优化

---

## 当我要求“开始写代码”时
请遵守以下流程：
1. 先说明本次只做哪个子模块
2. 先给目录树和文件清单
3. 再分文件生成代码
4. 不要一次性生成整个项目所有文件
5. 每完成一批文件，要说明用途和运行方式